/**
 * Supabase database types.
 *
 * Placeholder for Phase 1 (project setup). Phase 2 (Database) replaces this
 * with the generated/hand-written schema types for the `tickets` table,
 * matching the SQL migration in `supabase/migrations/`.
 */
export type Database = Record<string, unknown>;
